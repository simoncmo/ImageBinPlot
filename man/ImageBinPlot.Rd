% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ImageBinPlot.R
\name{ImageBinPlot}
\alias{ImageBinPlot}
\title{ImageBinPlot: Create a Spatial Binning Plot}
\usage{
ImageBinPlot(
  obj,
  feature,
  group.by = NULL,
  x_col = "x",
  y_col = "y",
  fov = NULL,
  x_min = NULL,
  x_max = NULL,
  y_min = NULL,
  y_max = NULL,
  bin_size = 10,
  max_multiplier = NULL,
  min = 0,
  max_quantile = "q75",
  type = "expression",
  assay = "Xenium",
  layer = "count",
  palette = c("viridis", "inferno", "magma", "plasma", "cividis", "mako", "rocket",
    "turbo"),
  palette_begin = 0,
  palette_end = 1,
  ident_alpha = 0.3,
  ident_pointsize = 1,
  filter_ident = NULL,
  feature_on_top = TRUE,
  flip_y = TRUE
)
}
\arguments{
\item{obj}{Seurat object}

\item{feature}{Gene to visualize}

\item{group.by}{Identity class to overlay (optional)}

\item{x_col}{Column name for x-coordinates (default: "x")}

\item{y_col}{Column name for y-coordinates (default: "y")}

\item{fov}{Field of view to visualize (optional)}

\item{x_min}{Minimum x-coordinate (optional)}

\item{x_max}{Maximum x-coordinate (optional)}

\item{y_min}{Minimum y-coordinate (optional)}

\item{y_max}{Maximum y-coordinate (optional)}

\item{bin_size}{Size of bins in micrometers (default: 10)}

\item{max_multiplier}{Maximum multiplier for color scale (deprecated)}

\item{min}{Minimum value for color scale (default: 0)}

\item{max_quantile}{Maximum quantile for color scale (default: "q75")}

\item{type}{Type of data to visualize ("expression" or "molecule", default: "expression")}

\item{assay}{Assay to use for fetching data (default: "Xenium")}

\item{layer}{Layer to use for fetching data (default: "count")}

\item{palette}{Color palette to use (default: c("viridis", "inferno", "magma", "plasma", "cividis", "mako", "rocket", "turbo"))}

\item{palette_begin}{Start of color palette (default: 0)}

\item{palette_end}{End of color palette (default: 1)}

\item{ident_alpha}{Alpha level for identity overlay (default: 0.3)}

\item{ident_pointsize}{Point size for identity overlay (default: 1)}

\item{filter_ident}{Optional filter for identity overlay}

\item{feature_on_top}{Whether to place feature layer on top of identity overlay (default: TRUE)}

\item{flip_y}{Whether to flip the y-axis (default: TRUE)}
}
\value{
A ggplot object representing the spatial binning plot
}
\description{
This function creates a spatial binning plot for a specified gene or molecule in a Seurat object.
It allows for optional overlay of identity classes and customization of bin size, color scale, and other parameters.
}
\examples{
ImageBinPlot(seurat_obj, feature = "GeneA", group.by = "cell_type", bin_size = 20)
ImageBinPlot(seurat_obj, feature = "GeneB", type = "molecule", bin_size = 50)
}
